{
  "name": "xun",
  "version": "0.2.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc && node build.js",
    "build:release": "tsc && node build.js --release",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "happy-dom": "^20.8.9",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "web-ext": "^10.1.0"
  }
}
